type RSVP @model @auth(rules: [{allow: public, provider: apiKey}])
{
  fullName: String!
  email: String
  phone: String
  guestCount: Int!
  attending: Boolean!
  arrivalInfo: String
  songRequest: String
  message: String
  language: String!
}